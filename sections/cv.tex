%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode
% No-photo wrapper: builds the resume without the profile photo, but keeps
% your private details (secrets.tex if present). Produces the private
% sections/output/cv.pdf — not committed to the repo.
%
% For the normal local build (with photo), use sections/resume.tex.
\def\noPhoto{1}
\input{resume.tex}
