%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode
% Public-version wrapper: forces use of secrets.example.tex (omits the full
% street address) and drops the profile photo. Produces the committed
% sections/output/resume-public.pdf, suitable for the public repo / README.
%
% For your normal local build (with full address and photo), use
% sections/resume.tex instead.
\def\publicMode{1}
\def\noPhoto{1}
\input{resume.tex}
